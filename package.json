{
  "name": "media-sdk-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:core": "npm run build --workspace=media-core",
    "build:wrappers": "npm run build --workspace=media-react && npm run build --workspace=media-native",
    "build:ui": "npm run build --workspace=media-ui-react && npm run build --workspace=media-ui-native",
    "dev:web": "npm run dev --workspace=web-app",
    "test:core": "npm run test --workspace=media-core"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  }
}
